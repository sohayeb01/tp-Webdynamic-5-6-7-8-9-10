[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
numReplicas = 1

[phases.setup]
nixPkgs = ["php81"]

[phases.build]
cmds = [
    "mkdir -p /app",
    "cp -r . /app"
] 